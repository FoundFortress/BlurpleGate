# BlurpleGate
BlurpleGate hooks into DiscordSRV and makes it simpler for users to link their Discord account. It uses Discord OAuth to verify users rather than requiring a code exchange.

## AI Usage
No Java code nor assets within this project were generated with AI. AI was used to assist with parsing documentation and generating GitHub Actions workflows.